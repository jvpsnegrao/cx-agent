{
  "mcpServers": {
    "cx-tools": {
      "command": "bun",
      "args": ["run", "../packages/cx-tools/src/server.ts"],
      "env": {
        "KHAL_DATABASE_URL": "postgresql://postgres:postgres@localhost:5432/omni",
        "CX_DEMO_URL": "http://localhost:3000",
        "CX_DEMO_TOKEN": "${CX_DEMO_TOKEN}"
      }
    }
  }
}
