{
  "name": "khal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "typecheck": "bun --filter '*' typecheck",
    "lint": "bunx biome check .",
    "lint:fix": "bunx biome check --write .",
    "test": "bun test",
    "db:generate": "cd packages/db && bunx drizzle-kit generate",
    "db:migrate": "bun packages/db/src/migrate.ts",
    "db:seed": "bun packages/db/src/seed.ts",
    "mcp:dev": "bun --watch packages/cx-tools/src/server.ts",
    "cron:reminder": "bun packages/cx-cron/src/reminder.ts",
    "demo": "bun --watch apps/cx-demo/src/server.ts",
    "demo:nowatch": "bun apps/cx-demo/src/server.ts",
    "setup": "./scripts/bootstrap.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.5",
    "typescript": "^5.7.3"
  },
  "packageManager": "bun@1.1.42",
  "engines": {
    "bun": ">=1.1"
  }
}
